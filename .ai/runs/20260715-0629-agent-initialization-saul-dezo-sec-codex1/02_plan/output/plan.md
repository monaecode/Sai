# Plan — Saul CTO initialization

1. Create an isolated `dezo/initialize-saul-cto` worktree and claim only Saul's
   new profile, registry row, and initialization run.
2. Scaffold the Codex runtime profile and replace generic claims with evidence
   gathered in the live Codex session.
3. Add the CTO skills, hooks, configurable roadmap, and Codex-specific operating
   profile without inventing an application stack or unattended automation.
4. Create public Slack `#dev`, publish initialization events, offer the operating
   profile, and introduce Saul in `#help-newagents`.
5. Verify JSON, scripts, semantic hierarchy, audit metadata, agent initialization,
   remote SHA, and GitHub CI; stop at human review before merge.
