# Runtime suites — Sai (`ceo`)

| Suite | Status | Capability file |
|---|---|---|
| **cursor** (primary) | active | `runtimes/cursor/tools.json` |
| claude | stub | `runtimes/claude/README.md` — not primary runtime |
| codex | stub | `runtimes/codex/README.md` — awaiting live Codex Desktop init |

Canonical manifest: root `tools.json` → `runtimes/cursor/tools.json`.

Invoke: `@sai` in Cursor Desktop or Cursor Automations (see
`runtimes/cursor/automation/profile.md`).
